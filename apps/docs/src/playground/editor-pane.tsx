'use client';

import type { EditorPaneProps } from './types';

export function EditorPane(_props: EditorPaneProps) {
  return (
    <div style={{ padding: '1rem', height: '100%', boxSizing: 'border-box' }}>
      <p>Editor — coming in phase 2</p>
    </div>
  );
}
