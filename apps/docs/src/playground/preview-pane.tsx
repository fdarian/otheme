import type { PreviewPaneProps } from './types';

export function PreviewPane(_props: PreviewPaneProps) {
  return (
    <div style={{ padding: '1rem', height: '100%', boxSizing: 'border-box' }}>
      <p>Preview — coming in phase 2</p>
    </div>
  );
}
