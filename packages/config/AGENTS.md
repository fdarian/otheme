# @repo/config

Shared TypeScript configuration workspace.

## Stack
- TypeScript
- Total TypeScript tsconfig presets

## Dev
- Consumed by workspace `tsconfig.json` files.

## Architecture
- `tsconfig*.json` - shared compiler baselines.
